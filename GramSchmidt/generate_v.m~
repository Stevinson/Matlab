%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% 25/11/13 Edward Stevinson
% Function that generates V

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

function V = generate_v(m, n  );

number_of_orthonormal_functions = 6;  

n = number_of_orthonormal_vectors;
                                      
V = zeros(n, length(x));

for i = 1:n
    for j = 1:length(x)
        V(i,j) = x(j)^


